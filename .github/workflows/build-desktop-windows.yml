name: Build Windows Desktop

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: Build only; do not publish OTA or GitHub Release assets
        required: false
        default: true
        type: boolean
      changelog:
        description: Release changelog
        required: false
        default: ""
        type: string
      content:
        description: OTA dialog content
        required: false
        default: ""
        type: string
      force:
        description: Force OTA update
        required: false
        default: false
        type: boolean

jobs:
  publish-contract:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: android/tools/publish

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up uv
        uses: astral-sh/setup-uv@v5

      - name: Test manifest contract
        run: uv run python test_manifest_contract.py

  build:
    runs-on: windows-latest
    needs: publish-contract
    outputs:
      version: ${{ steps.version.outputs.version }}
      artifact_name: ${{ steps.version.outputs.artifact_name }}
    defaults:
      run:
        working-directory: desktop

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.2'
          channel: stable
          cache: true

      - name: Read desktop version
        id: version
        shell: pwsh
        run: |
          $content = Get-Content pubspec.yaml -Raw
          if ($content -notmatch '(?m)^version:\s*([0-9]+\.[0-9]+\.[0-9]+)') {
            throw 'Cannot read version from desktop/pubspec.yaml'
          }
          $version = $Matches[1]
          "version=$version" >> $env:GITHUB_OUTPUT
          "artifact_name=loveace-windows-$version" >> $env:GITHUB_OUTPUT
          "Desktop version: $version"

      - name: Get dependencies
        run: flutter pub get

      - name: Build updater helper
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path build
          Push-Location windows\updater
          dart pub get
          dart compile exe bin\loveace_updater.dart -o ..\..\build\loveace_updater.exe
          Pop-Location

      - name: Analyze updater helper
        shell: pwsh
        run: |
          Push-Location windows\updater
          dart analyze
          Pop-Location

      - name: Analyze desktop project
        run: flutter analyze --no-fatal-infos --no-fatal-warnings

      - name: Test desktop project
        run: flutter test

      - name: Test updater helper
        shell: pwsh
        run: |
          Push-Location windows\updater
          dart test
          Pop-Location

      - name: Build Windows release
        shell: pwsh
        env:
          ANALYTICS_API_KEY: ${{ secrets.ANALYTICS_API_KEY }}
          ANALYTICS_SIGNING_SECRET: ${{ secrets.ANALYTICS_SIGNING_SECRET }}
          ANALYTICS_HASH_SALT: ${{ secrets.ANALYTICS_HASH_SALT }}
        run: >
          flutter build windows --release
          --dart-define=ANALYTICS_ENDPOINT=https://analyst-api.linota.cn/v1/events
          --dart-define=ANALYTICS_API_KEY=$env:ANALYTICS_API_KEY
          --dart-define=ANALYTICS_SIGNING_SECRET=$env:ANALYTICS_SIGNING_SECRET
          --dart-define=ANALYTICS_HASH_SALT=$env:ANALYTICS_HASH_SALT

      - name: Copy updater helper into release
        shell: pwsh
        run: |
          Copy-Item '${{ github.workspace }}\desktop\build\loveace_updater.exe' '${{ github.workspace }}\desktop\build\windows\x64\runner\Release\loveace_updater.exe' -Force

      - name: Install Inno Setup
        shell: pwsh
        run: |
          $innoUrl = 'https://jrsoftware.org/download.php/is.exe'
          $innoInstaller = "$env:TEMP\innosetup.exe"
          Invoke-WebRequest -Uri $innoUrl -OutFile $innoInstaller
          Start-Process -FilePath $innoInstaller -ArgumentList '/VERYSILENT', '/SUPPRESSMSGBOXES', '/NORESTART' -Wait

          $innoPath = 'C:\Program Files (x86)\Inno Setup 6'
          "$innoPath" >> $env:GITHUB_PATH

          $langUrl = 'https://raw.githubusercontent.com/jrsoftware/issrc/main/Files/Languages/ChineseSimplified.isl'
          $langPath = "$innoPath\Languages\ChineseSimplified.isl"
          Invoke-WebRequest -Uri $langUrl -OutFile $langPath

      - name: Download VC++ Redistributable
        shell: pwsh
        run: |
          $vcRedistUrl = 'https://aka.ms/vs/17/release/vc_redist.x64.exe'
          $vcRedistPath = '${{ github.workspace }}\desktop\build\windows\x64\runner\Release\vc_redist.x64.exe'
          Invoke-WebRequest -Uri $vcRedistUrl -OutFile $vcRedistPath

      - name: Create installer
        shell: pwsh
        env:
          APP_VERSION: ${{ steps.version.outputs.version }}
        run: |
          $buildDir = '${{ github.workspace }}\desktop\build\windows\x64\runner\Release'
          $outputDir = '${{ github.workspace }}\desktop\installer'
          New-Item -ItemType Directory -Force -Path $outputDir
          $env:BUILD_DIR = $buildDir
          $env:OUTPUT_DIR = $outputDir
          & 'C:\Program Files (x86)\Inno Setup 6\ISCC.exe' /DAPP_VERSION="$env:APP_VERSION" /DBUILD_DIR="$buildDir" /DOUTPUT_DIR="$outputDir" 'windows\inno-ci.iss'

      - name: Create internal update zip
        shell: pwsh
        run: |
          $version = '${{ steps.version.outputs.version }}'
          $releaseDir = '${{ github.workspace }}\desktop\build\windows\x64\runner\Release'
          $zipDir = '${{ github.workspace }}\desktop\update-package'
          New-Item -ItemType Directory -Force -Path $zipDir
          $zipItems = Get-ChildItem -Path $releaseDir | Where-Object { $_.Name -ne 'vc_redist.x64.exe' }
          Compress-Archive -Path $zipItems.FullName -DestinationPath "$zipDir\loveace-$version-win-x64.zip" -Force

      - name: Upload installer artifact
        uses: actions/upload-artifact@v5
        with:
          name: ${{ steps.version.outputs.artifact_name }}
          path: desktop/installer/loveace-${{ steps.version.outputs.version }}-setup.exe

      - name: Upload internal update package artifact
        uses: actions/upload-artifact@v5
        with:
          name: ${{ steps.version.outputs.artifact_name }}-package
          path: desktop/update-package/loveace-${{ steps.version.outputs.version }}-win-x64.zip

      - name: Confirm dry run
        run: |
          echo "Dry run: ${{ inputs.dry_run }}"
          echo "OTA/manifest publishing is skipped when dry_run is true."

  upload-manifest:
    runs-on: ubuntu-latest
    needs: build
    if: ${{ inputs.dry_run == false }}
    environment: S3

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Download installer artifact
        uses: actions/download-artifact@v5
        with:
          name: ${{ needs.build.outputs.artifact_name }}
          path: ./installer

      - name: Download internal update package artifact
        uses: actions/download-artifact@v5
        with:
          name: ${{ needs.build.outputs.artifact_name }}-package
          path: ./package

      - name: Set up uv
        uses: astral-sh/setup-uv@v5

      - name: Publish Windows release
        env:
          S3_ENDPOINT: ${{ secrets.S3_ENDPOINT }}
          S3_ACCESS_KEY: ${{ secrets.S3_ACCESS_KEY }}
          S3_SECRET_KEY: ${{ secrets.S3_SECRET_KEY }}
          S3_BUCKET: ${{ secrets.S3_BUCKET }}
          S3_REGION: ${{ secrets.S3_REGION }}
          CDN_BASE_URL: ${{ secrets.CDN_BASE_URL }}
        run: |
          uv run python cli.py release \
            --version "${{ needs.build.outputs.version }}" \
            --platform windows \
            --file "../../../installer/loveace-${{ needs.build.outputs.version }}-setup.exe" \
            --package-file "../../../package/loveace-${{ needs.build.outputs.version }}-win-x64.zip" \
            --content "${{ inputs.content }}" \
            --changelog "${{ inputs.changelog }}" \
            ${{ inputs.force && '--force' || '' }}
        working-directory: android/tools/publish
